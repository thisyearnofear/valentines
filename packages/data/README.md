# @lub-u/data

Reusable data SDK for querying The Graph and Supabase.  
Generates typed React Query hooks from .graphql query documents.

## Usage

First, generate types and hooks:

```sh
npm run codegen:data
```

Then, import from `@lub-u/data` in your frontend app.